name: validation_sdk
description: Remote validation config SDK for Flutter — sync validation.json, resolve tenant rules, validate fields.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
